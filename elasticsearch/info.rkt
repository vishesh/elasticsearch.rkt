#lang setup/infotab

(define name "elasticsearch")
(define version "0.1")
(define categories '(misc))

(define primary-file '("elasticsearch.rkt"))
(define blurb (list '(div "Racket bindings for Elasticsearch")))

(define release-notes  
  (list '(div "0.1: Basic ES API bindings")))

